# IVY Map 产品需求文档（PRD）

**一句话定位**：把「孩子该上哪所高中」这个问题，从家长的道听途说，变成一张可查、可比、可解释的数据地图。

| 项 | 值 |
|---|---|
| 项目代号 | IVY Map（常春藤择校地图） |
| 文档版本 | v1.0 |
| 日期 | 2026-07-29 |
| 场景 | 黑客松（48 小时可演示 MVP） |
| 状态 | 待评审 |

---

## 一、背景与问题

### 1.1 家长真正在问的问题

留学择校市场上所有现成工具（QS/US News 排名、各类留学中介的择校表）回答的都是**同一个方向**的问题：

> 「这所大学好不好？孩子够不够得着？」

但一个孩子在初二的家长，此刻能做的决策根本不是「选大学」——那是三年后的事。他此刻唯一能动的杠杆是：**今年秋天把孩子送进哪所高中**。

所以真实的问题是**反向**的：

> 「如果目标是布朗，我家在上海，孩子现在初二、走 IB 路线，那我今年该把他送进哪所学校？那所学校我进得去吗？」

目前没有任何产品直接回答这个问题。家长只能在家长群、中介口述、和各校自己发的「战报」海报之间拼凑，而这些信息源全部存在系统性失真（见 1.2）。

### 1.2 现有信息源的三个系统性失真

| 失真 | 表现 | 后果 |
|---|---|---|
| **Offer 膨胀** | 学校战报报的是「录取 offer 数」而非「实际入读人数」。一个学生可同时拿 5–8 个 offer，头部学校尤甚。 | 「XX 中学斩获 30 枚藤校 offer」可能只对应 6–8 个学生。家长把 30 当成 30。 |
| **大校偏差** | 毕业生 600 人的学校，绝对录取数天然碾压毕业生 90 人的小校。 | 家长选了「总数大」的学校，实际人均命中率反而更低。 |
| **门槛盲区** | 榜单不告诉你这所学校**招不招你家孩子**（外籍要求、学籍/户籍、中考分数线、插班年级窗口）。 | 家长研究三个月，报名时才发现根本没资格。 |

**这三个失真就是 IVY Map 的产品切口。** 我们不是又做一个榜单，我们做的是把榜单**修正到可以用来做决策**。

---

## 二、产品定位与差异化

### 2.1 定位

**面向留学家庭的「反向择校推导引擎」**——以目标大学为起点，反推到国内高中，并给出可行性与下一步动作。

### 2.2 与现有方案的差异

| | 排名类工具（QS / US News） | 学校战报 / 家长群 | 留学中介 | **IVY Map** |
|---|---|---|---|---|
| 方向 | 高中 → 大学 | 单校自证 | 一对一咨询 | **大学 → 高中（反推）** |
| 口径 | 学术声誉 | Offer 数（膨胀） | 顾问经验 | **去重人数 + 人均密度** |
| 可比性 | 大学间可比 | 校际不可比 | 不可比 | **同赛道、同口径、可比** |
| 门槛信息 | 无 | 无 | 有但不透明 | **结构化、可筛选** |
| 成本 | 免费 | 免费 | 3–30 万/人 | 免费 / 增值 |

### 2.3 三条护城河（按建立难度排序）

1. **口径**（当天可建立）：offer→人数去重、人均密度归一化。这是方法论，写进文档就成立。
2. **门槛数据**（数周）：各校招生资格的结构化整理。极其琐碎，因此别人懒得做。
3. **纵向数据**（数年）：多年录取数据的连续沉淀，越久越值钱。

---

## 三、目标用户与使用场景

### 3.1 主用户：规划期家长（P0）

- **画像**：孩子 6–9 年级（小升初到中考前后），家庭年收入 100 万+，已确定走国际路线但未确定学校。
- **认知状态**：知道「藤校」，不知道「Feeder School」；能背出 QS 前 20，说不出上海哪所学校 IB 均分更高。
- **核心焦虑**：择校是一次性、不可逆、且窗口只有一两个月的决策。选错就是四年 + 一百万。
- **JTBD**：「帮我在三周内，把 20 所候选高中缩到 3 所，并告诉我每所我进得去的概率和下一步该做什么。」

### 3.2 次用户：国际教育顾问（P1）

- **画像**：独立升学顾问、中介机构规划师。
- **JTBD**：「给我一份能直接发给客户的、有出处、看起来专业的择校依据。」
- **价值**：他们是天然的传播节点——一个顾问服务 30 个家庭。

### 3.3 反向用户：国际学校招生办（P2，商业化入口）

看到自己在榜单上的位置，会主动来「补充数据 / 更正数据」。这是数据飞轮的起点，也是未来的付费方。

### 3.4 典型场景（Demo 主线）

> 陈女士，上海浦东，女儿初二，数学强、英语中上，家里预算每年 30 万以内，目标「Top 30，最好是布朗」。
>
> 她打开 IVY Map → 点击地图上的布朗大学 → 右侧中国地图亮起热力 → 切换到「上海 + IB 赛道」→ 看到 6 所学校的排序 → 打开滑杆从「规模优先」调到「概率优先」，排序发生变化 → 点开第 2 名，看到该校**入学门槛卡片**：不限外籍、需参加自主招生考试、报名窗口 3 月、学费 20 万/年 → 加入对比 → 导出 PDF 给孩子爸爸看。

**整个过程 90 秒。** 这是我们要在评委面前跑通的路径。

---

## 四、核心概念与术语

| 术语 | 定义 | 为什么重要 |
|---|---|---|
| **Feeder School（生源校）** | 持续向某所大学输送录取学生的国内高中。 | 产品的核心对象。 |
| **Offer 数 vs 录取人数** | Offer 数含一人多 offer；录取人数为去重后的**人头**。 | 口径分歧是行业最大误导源。 |
| **赛道（Track）** | AP / IB / A-Level / 其他（如加拿大 OSSD、日本课程）。 | 不同赛道基本不可跨比，混在一起排序等于噪声。 |
| **规模强度（Volume）** | 加权后的录取人数绝对值。 | 回答「哪所学校送进去的人最多」。 |
| **人均密度（Density）** | 录取人数 / 该赛道毕业生数。 | 回答「我家孩子在这里的命中率最高」。 |
| **择校杠杆率（Leverage）** | 某大学的中国录取在高中间的集中程度（HHI）。 | 回答「择校这件事，对这所大学值不值得折腾」。 |
| **中国友好度（CAI）** | 某大学对中国大陆学生的开放程度综合评级。 | 回答「这所大学是不是根本不怎么招大陆」。 |
| **可行性闸门（Feasibility Gate）** | 国籍、学籍/户籍、年级窗口、考试要求等硬性准入条件。 | 回答「我家孩子能不能报」。 |
| **置信等级（L1/L2/L3）** | 数据来源可靠性分层。 | 产品的诚信底座，也是评委必问的问题。 |

---

## 五、产品原则

1. **每个数字都要有出处。** 界面上任何一个数值，点击可展开来源、年份、置信等级。宁可标「数据不足」，不可编造。
2. **默认口径就是正确口径。** 用户不需要理解 offer 膨胀，我们默认给去重人数，把 offer 数放在次要位置。
3. **只在可比的东西之间排序。** 不同赛道、不同置信等级的数据不混排。
4. **给动作，不只给结论。** 每个推荐结果必须附带「下一步做什么」（报名窗口、考试类型、准备周期）。
5. **不做承诺。** 全站不出现「录取概率」这类预测性表述，只呈现历史事实与统计口径。合规红线（见 §14）。

---

## 六、功能需求

### 6.0 优先级说明

- **P0**：黑客松 48 小时内必须完成，Demo 主线依赖。
- **P1**：Demo 有则加分，无则不影响主线。
- **P2**：路演时口头描述的 Roadmap。

---

### 6.1 模块一：双屏联动地图（P0）· 视觉主锤

**目标**：在 3 秒内让评委和用户理解「这是一个反向工具」。

- 左屏：**全球大学地图**，覆盖美 / 英 / 加 / 日 / 港。大学以点标记，点大小 = 该校近三年中国大陆录取总量。
- 右屏：**中国生源地图**，默认灰。
- **核心交互**：点击左屏任一大学 → 右屏中国地图按生源校所在城市点亮热力，同时右侧面板加载该校的 Feeder 榜单。
- 反向交互（P1）：点击右屏某城市 → 左屏高亮该城市孩子近三年主要去向的大学。

**验收标准**
- 点击到热力渲染完成 < 500ms（本地数据，无需请求后端亦可）。
- 至少 10 所美国 Top 30 大学 + 20 所国内头部国际高中数据可点。
- 无数据的大学显示为空心点，点击提示「暂无充分数据」，不得静默失败。

---

### 6.2 模块二：大学录取画像（P0）

点击大学后，右侧顶部卡片展示：

1. **近三年中国大陆录取趋势**（柱状图，标注是否为 offer 口径）
2. **生源地域分布**（Top 5 城市及占比）
3. **中国友好度 CAI**（A–E 五级，可展开看构成维度）
4. **择校杠杆率 Leverage**（高 / 中 / 低 + 一句话解读）

> **Leverage 是本模块的差异化点。** 示例文案：
> 「布朗大学近三年中国录取的 68% 集中在 6 所高中 → **择校杠杆：高**。对这所大学而言，进对高中的影响，可能大于孩子在原学校多刷两分标化。」
>
> 反例文案：
> 「多伦多大学近三年中国录取分布在 140+ 所高中 → **择校杠杆：低**。这所大学不太看你从哪所高中来，钱和精力建议花在别处。」

**验收标准**
- CAI 与 Leverage 均可展开看到计算依据与原始数据。
- 每张卡片右上角显示置信徽章（L1 官方 / L2 权威二手 / L3 推断）。

---

### 6.3 模块三：Feeder School 榜单（P0）· 核心杀手锏

**默认排序：Feeder Score**（三维合成，算法见 [`docs/scoring.md`](docs/scoring.md)）。

**筛选维度**
- 城市（上海 / 北京 / 深圳 / 广州 / 杭州 / 南京 / …）
- 赛道（AP / IB / A-Level / 其他）
- 学校性质（公办国际部 / 民办国际化学校 / 外籍人员子女学校）
- 是否可申请（可行性闸门开关，见 6.5）

**「规模 ↔ 概率」滑杆（本产品的签名交互）**

一个 0–100 的滑杆，控制 Feeder Score 中 Volume 与 Density 的权重：

- 拉到最左（**规模优先**）：按绝对录取人数排 → 大校靠前。适合「我想去人多、氛围强、校友网密的地方」。
- 拉到最右（**概率优先**）：按人均密度排 → 小而精的学校浮上来。适合「我只关心我家孩子的命中率」。

拖动时榜单实时重排，并有一行动态解读文案：

> 「你正在按**概率优先**排序。第 1 名从 XX 中学换成了 XX 学校——后者近三年送进布朗的人数只有前者的一半，但毕业生规模只有 1/4。」

**这是整个 Demo 的高光时刻**：一个动作，同时展示了数据深度、方法论、和对家长真实分歧的理解。

**榜单每行展示**
| 排名 | 学校 | 赛道 | 近三年录取（人） | 人均密度 | 学费 | 可申请 | 置信 |
|---|---|---|---|---|---|---|---|
| 1 | 示例学校 A | IB | 12 | 4.2% | 22 万/年 | ✅ | L1 |
| 2 | 示例学校 B | IB | 7 | 7.8% | 18 万/年 | ⚠️ 限外籍 | L1 |

> 表中数值为格式示意，非真实数据。真实数据以录入源为准。

**验收标准**
- 任意「城市 × 赛道」组合下的重排 < 200ms。
- 结果为空时给出建议（「上海 + A-Level 冲布朗的样本不足，试试放宽到长三角，或看看 IB 赛道」），不得只显示空表。
- 每一行的录取人数可点击展开：来源链接、年份拆分、offer/人数口径标注。

---

### 6.4 模块四：学校深度卡片（P0）

点击榜单任一行展开。分四段，**顺序对应家长的决策顺序**：

1. **它送谁去了哪** —— 近三年 Top 10 去向大学、赛道分布、毕业生规模。
2. **你进得去吗** —— 可行性闸门详情：国籍要求 / 学籍户籍 / 招生年级 / 入学考试形式 / 报名窗口 / 近年录取率（若有）。
3. **要花多少钱** —— 学费、住宿、预估三年总投入区间。
4. **下一步做什么** —— 结构化行动项，例如「3 月上旬报名，考试含数学（英文卷）+ 英语 + 面谈，建议提前 6 个月准备」。

**验收标准**：第 2 段「你进得去吗」为**必填字段**，缺失时显示「门槛信息待补充」并附反馈入口，不得留空。

---

### 6.5 模块五：可行性闸门（P0）· 被所有竞品忽略的一环

顶部一个轻量表单（可折叠，不填不影响浏览）：

- 孩子国籍 / 是否持外籍或港澳台身份
- 学籍所在城市 + 是否本市户籍
- 当前年级
- 目标入学年份

填写后：**榜单中不符合硬性条件的学校自动置灰并标注原因**（如「仅招收外籍人员子女」「需本市中考成绩且分数线约 XXX」「仅在 7 年级 / 9 年级开放入学」）。

> **产品价值**：这一个功能就能省掉家长两个月的无效研究。也是最容易被评委记住的「你们居然想到了这个」的点。

**验收标准**：置灰必须给出**具体原因**而非仅标记不可用；表单可随时清空恢复全量视图。

---

### 6.6 模块六：AI 择校顾问（P1）· 技术加分项

自然语言输入框：

> 「我家在杭州，孩子初二，走 IB，想冲 Top 20，预算每年 25 万以内」

LLM 将其解析为结构化查询（城市、年级、赛道、目标大学集合、预算上限），调用与 UI 完全相同的查询接口，返回：

- 3 所推荐学校 + 每所的推荐理由（**理由必须引用真实数据字段**，不得由模型自由发挥）
- 一条时间轴：从现在到入学的关键节点
- 一句坦诚的风险提示

**约束（重要）**：LLM 只负责「自然语言 → 结构化查询」和「数据 → 自然语言解释」两端，**中间的检索和排序全部走确定性代码**。模型不接触也不生成任何数值。这条边界要写在 Demo 讲稿里——评委一定会问「你们怎么防止 AI 编数据」。

---

### 6.7 模块七：对比与导出（P1）

- 勾选 2–4 所学校 → 并排对比表（去向、密度、门槛、费用、行动项）。
- 导出为一页 PDF / 长图，带数据来源与免责声明。

> 家长的真实工作流是「拿去说服配偶 / 发给顾问」。可分享的产物 = 免费传播。

---

### 6.8 P2 / 路线图功能

| 功能 | 说明 |
|---|---|
| **华尔街地图** | 大学 → 顶级投行/咨询/科技公司的 Target School 榜单，与现有数据串成「高中 → 大学 → 职业」完整路径。详见 §15。 |
| **时间轴规划器** | 输入当前年级，生成到入学的逐月任务清单（考试、竞赛、报名、面试）。 |
| **数据众包与纠错** | 家长/校方提交更正，进入审核队列，采纳后标注贡献者。数据飞轮起点。 |
| **学校订阅** | 关注某校，新数据/新战报发布时推送。 |
| **趋势预警** | 「某校近三年藤校录取连续下滑」这类信号。 |

---

## 七、评分算法（摘要）

完整推导、参数与示例计算见 [`docs/scoring.md`](docs/scoring.md)。此处只列核心定义。

对学校 `S`、大学 `U`、赛道 `T`：

**时间权重**（近的年份更重要）
```
w(最近一届) = 0.5, w(前一届) = 0.3, w(再前一届) = 0.2
```

**规模强度**
```
Volume(S,U,T) = Σ_y w(y) · Admits(S,U,T,y)
```

**人均密度**
```
Density(S,U,T) = Σ_y w(y) · Admits(S,U,T,y) / Σ_y w(y) · Grads(S,T,y)
```

**Feeder Score**（α 由「规模 ↔ 概率」滑杆控制，默认 0.5）
```
Score = norm(Volume)^α · norm(Density)^(1-α)
```

**择校杠杆率**（对大学 U，基于生源校份额的 HHI）
```
Leverage(U) = Σ_S share(S,U)²        // 高 > 0.15，中 0.05–0.15，低 < 0.05
```

**Offer → 人数折算**：源数据为 offer 口径时，按学校当年「总 offer 数 / 毕业生数」估算人均 offer 系数 `k(S,y)`，取 `Admits ≈ Offers / k`，并将该条记录置信等级下调一级。若学校同时公布人数口径，**始终优先采用人数口径**。

---

## 八、信息架构

```
首页（双屏地图）
├── 左：全球大学地图 ──点击大学──┐
├── 右：中国生源热力图 ←────────┘
└── 右侧抽屉面板
    ├── ① 大学画像卡（趋势 / 地域 / CAI / Leverage）
    ├── ② 筛选条（城市 · 赛道 · 性质 · 可申请）
    ├── ③ 「规模 ↔ 概率」滑杆
    ├── ④ Feeder 榜单 ──点击行──→ 学校深度卡片（4 段式）
    └── ⑤ 对比栏（勾选 2–4 所 → 对比表 → 导出）

顶部
├── 可行性闸门表单（折叠）
└── AI 顾问输入框（P1）
```

**移动端（P1）**：双屏改为上下切换的单屏 Tab，榜单为主视图。黑客松 Demo 以桌面 1440×900 为准。

---

## 九、数据模型

SQLite，7 张表。字段命名保持可读，避免缩写。

```sql
-- 大学
university(
  id, name_cn, name_en, country, city, lat, lng,
  world_rank, us_news_rank,
  cai_grade,              -- 中国友好度 A–E
  leverage_hhi,           -- 择校杠杆率
  updated_at
)

-- 国内高中
school(
  id, name_cn, name_en, city, district, lat, lng,
  school_type,            -- public_intl_dept | private_intl | foreign_nationals
  tracks,                 -- JSON: ["IB","AP"]
  tuition_cny_per_year,
  boarding,               -- bool
  updated_at
)

-- 每届毕业生规模（密度的分母，缺它整个算法失效）
school_cohort(
  school_id, year, track,
  graduates_count,
  total_offers,           -- 用于估算 offer→人数系数
  source_id
)

-- 录取事实表（核心）
admission(
  id, school_id, university_id, year, track,
  admits_count,           -- 去重人数（首选口径）
  offers_count,           -- offer 口径（备用）
  count_basis,            -- 'admits' | 'offers' | 'estimated'
  confidence,             -- 'L1' | 'L2' | 'L3'
  source_id
)

-- 可行性闸门
school_requirement(
  school_id,
  nationality_required,   -- none | foreign | hk_mo_tw | foreign_or_pr
  hukou_required,         -- none | local_city | local_district
  entry_grades,           -- JSON: [7, 9]
  exam_type,              -- JSON: ["math_en","english","interview"]
  application_window,     -- 'MM-DD~MM-DD'
  notes
)

-- 来源（每条数据都必须挂一个）
source(
  id, source_type,        -- official | media | report | crowdsourced
  title, url, published_at, captured_at, confidence
)

-- 中国友好度构成（可展开查看）
university_cai(
  university_id, year,
  mainland_admits_total,
  distinct_feeder_schools,
  has_china_rep,          -- bool
  aid_for_intl,           -- need_blind | need_aware | none
  notes
)
```

**约束**
- `admission` 与 `school_cohort` 均**强制外键到 `source`**，无源数据不得入库。这一条从第一行代码就要执行，否则最后没法回答评委「数据哪来的」。
- 同一 `(school, university, year, track)` 若存在多来源，保留全部记录，展示时取置信等级最高者，冲突时在 UI 标注「多来源不一致」。

---

## 十、技术架构

延续原方案的轻量路线，黑客松优先保证 48 小时内可跑通。

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | 原生 HTML + ECharts 5 + Alpine.js | ECharts 的 geo/map 与热力图开箱即用；Alpine 足够驱动筛选和滑杆，省掉构建链。 |
| 后端 | Flask + SQLite | 单文件部署，数据即代码库的一部分，评委可当场看数据表。 |
| 打分 | Python 纯函数（`scoring.py`） | 与 Web 层解耦，可单测。滑杆参数走 URL query，可分享链接。 |
| AI 顾问（P1） | Claude API（`claude-sonnet-5`），function calling 出结构化查询 | 只做 NL↔结构化转换，不生成数值。 |
| 部署 | 单机 / Render / Fly.io | 演示够用。 |

### 关键 API

```
GET  /api/universities                        → 地图打点数据
GET  /api/university/<id>                     → 画像卡（趋势/CAI/Leverage）
GET  /api/feeders?university_id=&city=&track=&alpha=0.5&nationality=&hukou=&grade=
                                              → 榜单（含置灰原因）
GET  /api/school/<id>                         → 深度卡片四段内容
POST /api/advisor                             → NL → 结构化查询 → 榜单 + 解释（P1）
GET  /api/source/<id>                         → 来源详情（每个数字可溯源）
```

`/api/feeders` 是全站核心接口，UI 与 AI 顾问共用同一个实现——保证两者结果永远一致。

### 前端性能

首屏一次性加载全量精简数据集（预计 < 300KB JSON），筛选与重排全部在客户端完成。数据规模在 MVP 阶段完全撑得住，换来的是零延迟的滑杆手感——**这个手感就是 Demo 的成败**。

---

## 十一、数据范围与获取

详见 [`docs/data-sources.md`](docs/data-sources.md)。MVP 范围：

| 维度 | MVP 目标 | 说明 |
|---|---|---|
| 大学 | 美国 Top 30 中的 10 所 | 优先 8 所藤校 + 2 所高关注度（如 NYU、UCB） |
| 高中 | 20 所国内头部国际化学校 | 上海 8 · 北京 5 · 深圳 3 · 广州 2 · 杭州/南京 2 |
| 年份 | 近 3 届 | 2023 / 2024 / 2025 届 |
| 赛道 | AP · IB · A-Level | 三条主赛道全覆盖 |
| 门槛数据 | 20 所全部填齐 | **不允许留空**，这是差异化所在 |

**采集方式**：学校官网/官微升学战报（L1）→ 学校年度升学报告 PDF（L1）→ 权威媒体年度汇总（L2）→ 公开可查的其他统计（L3）。全部为公开信息，不涉及个人身份数据。

**数据录入是本项目最大的时间风险**（见 §13），必须在 Day 0 就并行启动，不能等前端做完。

---

## 十二、成功指标

### 黑客松阶段（唯一真正的指标是「评委记不记得住」）

| 指标 | 目标 |
|---|---|
| Demo 主线跑通 | 90 秒内完成 §3.4 全流程，零报错 |
| 数据完整度 | 10 所大学 × 20 所高中 × 3 年，覆盖率 ≥ 80%，门槛字段 100% |
| 可溯源率 | 界面上 100% 的数值可点击看到来源 |
| 「规模↔概率」滑杆 | 拖动时排名发生**可见且可解释**的变化（至少 2 组数据能演示反转） |

### 产品化阶段（P1+）

| 指标 | 定义 |
|---|---|
| 决策深度 | 单次会话中打开学校深度卡片 ≥ 3 所的用户占比 |
| 产物导出率 | 导出对比表/PDF 的会话占比（代表「真的拿去用了」） |
| 数据纠错提交量 | 家长/校方主动提交更正的条数（数据飞轮启动信号） |
| 顾问留存 | 顾问角色的周活跃复访率 |

---

## 十三、48 小时排期

前提：3 人小队（1 数据 · 1 后端+算法 · 1 前端）。**数据必须从第 0 小时开始，它是关键路径。**

| 时段 | 数据 | 后端 / 算法 | 前端 |
|---|---|---|---|
| **H0–H8** | 定 10 大学 + 20 高中名单；搭录入表格模板（含 source 列）；开录 | 建库、建表、跑通 Flask 骨架、写死 mock 数据 | ECharts 双地图跑通，假数据点亮热力 |
| **H8–H20** | 录完 3 年录取数据（约 600 条） | `scoring.py`：Volume / Density / Score / Leverage + 单测 | 榜单组件、筛选条、右侧抽屉 |
| **H20–H32** | 录门槛数据（20 所 × 全字段） | `/api/feeders` 全参数打通（含闸门置灰） | **滑杆 + 实时重排**（最高优先级）、学校深度卡片 |
| **H32–H40** | 交叉核对、补 source 链接、标 L1/L2/L3 | CAI / Leverage 计算落库；空结果兜底文案 | 可行性闸门表单、对比与导出、置信徽章 |
| **H40–H46** | 抽查 20 条数据回溯来源 | AI 顾问（P1，做不完就砍） | 视觉打磨、空/错态、移动端降级 |
| **H46–H48** | — | 全员：跑三遍 Demo、写讲稿、准备答辩 Q&A | |

**砍功能顺序（时间不够时按此顺序砍）**：AI 顾问 → 导出 PDF → 对比表 → 反向点击（城市→大学）→ 移动端。

**绝不能砍**：双屏联动、榜单、滑杆、可行性闸门、数据可溯源。这五个是产品的全部意义。

---

## 十四、风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| **数据录入超时**（最大风险） | 没数据 = 没产品 | Day 0 并行启动；先保证「布朗 + 上海 + IB」这一条 Demo 主线数据 100% 完整，其余按主线外围逐级补 |
| **口径不一致**（有的校报 offer，有的报人数） | 排序失真，被评委当场质疑 | `count_basis` 字段强制标注；UI 明示口径；跨口径比较时降置信等级并加警示 |
| **学校数据缺失**（部分校不公布毕业生数） | 密度算不出来 | 密度显示「—」而非 0；该校在「概率优先」排序下降权并标注「分母缺失」，绝不猜 |
| **法律/合规**：数据引用、学校声誉 | 潜在纠纷 | 全部使用公开信息；标注来源与日期；提供更正入口；全站免责声明（见下）；不做录取概率预测 |
| **被解读为「贩卖焦虑」** | 舆论风险 | 产品语言坚持中性、事实导向；**主动呈现「择校杠杆低」的大学**，明确告诉用户哪些情况下不必折腾——这反而是最强的可信度证明 |
| **LinkedIn 等来源的 ToS 风险**（华尔街地图） | 未来阶段合规问题 | v2 阶段改用公开年度报告与行业统计聚合，不做个人档案抓取 |

### 免责声明（全站页脚 + 导出物）

> 本站数据来自学校官方公开发布、公开媒体报道及公开行业报告，由 IVY Map 整理，可能存在滞后或误差，不构成任何录取承诺或升学建议。数据如有出入，欢迎通过反馈入口提交更正。

---

## 十五、路线图：从「藤校地图」到「华尔街地图」

原始构想中的职业版延展，是本产品最值钱的部分——它把一次性的择校决策，变成一条可持续追踪的**人生路径图**。

### v2 目标

将同一套「反推 + 三维评分 + 集中度」的方法论，从「高中→大学」平移到「大学→雇主」：

- **Target School 榜单**：顶级投行 / 咨询 / 大厂按录用人数排序的目标院校。
- **中国留学生友好度**：是否有中国区校招、往年录用中国籍学生数、是否 sponsor 工作签证（这一条对中国家庭是决定性的，且现有信息几乎为零）。
- **三段路径贯通**：`高中 → 目标大学 → 目标雇主`，一条链上算总杠杆率——回答终极问题：「为了进高盛，我今年该把孩子送进哪所高中？」

### v2 数据可行性

投行 Target School 无官方名单，但可通过以下公开来源构建：各机构年度校招报告、公开的行业统计（如 WSO 等论坛的年度汇总）、专业机构（如 College Transitions）的年度报告。**MVP 范围可收窄到 3–5 家顶级投行 × 英美 Top 20 大学**，规模上完全可控。

> 合规上，v2 阶段应避免抓取个人职业档案，改以聚合报告与机构公开数据为主——既降低风险，也不影响榜单结论。

### 分阶段

| 阶段 | 内容 |
|---|---|
| v1（黑客松） | 高中 → 美国 Top 30（10 所），三赛道，可行性闸门 |
| v1.5 | 扩到英/加/港/日；覆盖 60+ 高中；上线数据众包与纠错 |
| v2 | 华尔街地图：3–5 家投行 × 英美 Top 20，路径贯通 |
| v3 | 全路径规划器：从当前年级到目标雇主的逐年任务轴；顾问版工作台（商业化） |

---

## 附录 A：相关文档

- [`docs/scoring.md`](docs/scoring.md) —— Feeder Score / CAI / Leverage 完整算法与示例计算
- [`docs/data-sources.md`](docs/data-sources.md) —— 数据来源、采集流程、置信分层与合规边界
- [`docs/demo-script.md`](docs/demo-script.md) —— 3 分钟路演讲稿与 Q&A 准备

## 附录 B：本版相对初稿的主要变更

| # | 变更 | 动机 |
|---|---|---|
| 1 | 引入 **Volume / Density / Leverage 三维评分**，取代单一 offer 数排序 | 修正 offer 膨胀与大校偏差，这是本产品的方法论护城河 |
| 2 | 新增 **「规模 ↔ 概率」滑杆** | 把方法论变成一个 3 秒内可感知的交互，也是 Demo 高光 |
| 3 | 新增 **择校杠杆率（Leverage）** | 反直觉洞察：主动告诉用户哪些大学不必在择校上折腾，建立可信度 |
| 4 | 新增 **可行性闸门** | 所有竞品的盲区；直接省掉家长数月无效研究 |
| 5 | 新增 **置信等级与全站可溯源** | 数据诚信底座，也是评委必问项的正面回答 |
| 6 | 新增 **学校深度卡片四段式**（去向 / 门槛 / 费用 / 下一步） | 从「给结论」升级到「给动作」 |
| 7 | 新增 **AI 顾问**（P1），并明确「模型不碰数值」的边界 | 技术加分项 + 提前防住「AI 会不会编数据」的质疑 |
| 8 | 明确 **数据是关键路径**，排期从 H0 并行启动 | 初稿低估了数据录入的工作量，这是项目最大的失败风险 |
| 9 | 补充 **合规与反焦虑立场** | 教育类产品的舆论与法律风险不可忽视 |
| 10 | 华尔街地图细化为 **v2 分阶段方案**，并调整数据来源以规避 ToS 风险 | 保留最大想象空间，同时给出可落地路径 |
